import TypeCatalogPage from "@/features/catalog/components/TypeCatalogPage";

const Series = () => (
  <TypeCatalogPage title="Series" type="series" emptyMessage="No series found." />
);

export default Series;
